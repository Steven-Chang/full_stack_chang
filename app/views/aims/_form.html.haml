= form_with(model: aim, local: true) do |form|
  - if aim.errors.any?
    #error_explanation
      %h2
        = pluralize(aim.errors.count, "error")
        prohibited this aim from being saved:
      %ul
        - aim.errors.full_messages.each do |message|
          %li= message
  .form-group.row.align-items-center
    %label.col-lg-3.form-control-label.d-flex
      Description
    .col-lg-9
      = form.text_field :description, class: 'form-control', required: true
  .text-right
    = form.submit class: 'btn btn-gradient-01'