<%= form_with(model: payment_summary, local: true) do |form| %>
  <% if payment_summary.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(payment_summary.errors.count, "error") %> prohibited this payment_summary from being saved:</h2>

      <ul>
      <% payment_summary.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
