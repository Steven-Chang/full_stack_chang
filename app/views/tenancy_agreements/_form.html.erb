<%= form_with(model: tenancy_agreement, local: true) do |form| %>
  <% if tenancy_agreement.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tenancy_agreement.errors.count, "error") %> prohibited this tenancy_agreement from being saved:</h2>

      <ul>
      <% tenancy_agreement.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
